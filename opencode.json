{
  "$schema": "https://opencode.ai/config.json",
  "instructions": ["AGENTS.md"],
  "command": {
    "train": {
      "template": "Run the full ZSL-cWGAN-GP training pipeline:\npython main.py",
      "description": "Full training (CLIP, 150 epochs)"
    },
    "train-legacy": {
      "template": "Run the original GloVe-based monolithic script:\npython ZSLcWGAN-GP.py",
      "description": "Legacy GloVe training (50 epochs)"
    },
    "train-quick": {
      "template": "Run quick 5-epoch training test with CLIP embeddings:\npython test_training.py",
      "description": "Quick CLIP sanity check (5 epochs)"
    },
    "test-clip": {
      "template": "Run the CLIP embedding quality test suite:\npython test_clip.py",
      "description": "CLIP embedding test suite"
    },
    "demo": {
      "template": "Launch the Gradio interactive demo:\npython app.py",
      "description": "Gradio ZSL prediction demo"
    },
    "tensorboard": {
      "template": "Start TensorBoard:\ntensorboard --logdir=runs",
      "description": "View training curves"
    }
  }
}
